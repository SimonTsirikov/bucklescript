'use strict';

var List = require("../../lib/js/list.js");

function v(x) do
  return --[ tuple ]--[
          List.length(x),
          List.length(x)
        ];
end

var L = --[ alias ]--0;

exports.L = L;
exports.v = v;
--[ No side effect ]--
