'use strict';

var $$Buffer = require("../../lib/js/buffer.js");

var foo = $$Buffer.contents;

function bar(str) do
  return Buffer.from(str);
end

exports.foo = foo;
exports.bar = bar;
--[ No side effect ]--
